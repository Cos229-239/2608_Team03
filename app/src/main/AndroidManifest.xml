<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MICROPHONE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!--
        READ_MEDIA_IMAGES was declared and never needed. Documents and photographs come in
        through ActivityResultContracts.OpenDocument, which is the system document picker:
        it hands back a per-file grant for the one thing the person chose, and asks for no
        standing permission at all. Declaring the broad one meant the install screen asked
        a family archive to be trusted with every photograph on the phone, to read none of
        them. On Android 14 it also opened the partial-access flow, which this app has no
        code for and no use for.
    -->

    <uses-feature android:name="android.hardware.microphone" android:required="true" />

    <application
        android:name=".ArvApp"
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Arv">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize"
            android:theme="@style/Theme.Arv">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- CAP-2: keeps a 45-minute interview alive when the screen locks. -->
        <service
            android:name=".feature.record.RecordingService"
            android:exported="false"
            android:foregroundServiceType="microphone" />

    </application>

</manifest>
